{
  "name": "peredelano-profile-gh-pages",
  "private":"true",
  "version": "0.0.0",
  "scripts": {
    "build": "rimraf dist && node build.js",
    "deploy": "node deploy.js"
  },
  "dependencies": {

  },
  "devDependencies": {
    "copyfiles": "2.4.1",
    "gh-pages": "5.0.0",
    "peredelano-profile": "0.0.1",
    "rimraf": "5.0.0"
  }
}
